{
	"name": "zipcode-city",
	"version": "1.0.0",
	"description": "Get city and state from a zipcode. Currently german zipcodes only.",
	"main": "build/module.js",
	"scripts": {
		"test": "node test/test.js",
		"build": "tsc -p . && cp src/data.*.json build/"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/Cognigy/CustomModules"
	},
	"author": "Cognigy GmbH",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^12.12.17",
		"typescript": "^3.7.3"
	}
}
