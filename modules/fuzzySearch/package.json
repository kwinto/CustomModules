{
	"name": "FuzzySearch",
	"version": "1.0.0",
	"description": "This module adds a fuzzy logic node to Cognigy.AI.",
	"author": "Cognigy GmbH",
	"scripts": {
		"transpile": "tsc -p .",
		"zip": "zip fuzzysearch.zip build/* package.json package-lock.json README.md icon.png icon-large.png",
		"build": "npm run transpile && npm run lint && npm run zip"
	},
	"license": "MIT",
	"keywords": [],
	"main": "build/module.js",
	"dependencies": {
		"fuse.js": "^3.4.5"
	},
	"devDependencies": {
		"@types/node": "^12.0.8",
		"typescript": "^3.5.2"
	}
}
